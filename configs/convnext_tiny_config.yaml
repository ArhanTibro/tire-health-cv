# convnext_tiny transfer-learning config.
# Placeholder values - notebook 09 overwrites this file with whatever
# actually wins its grid search, so do not hand-tune these.
model_name: convnext_tiny
image_size: 224
batch_size: 16
epochs: 30
learning_rate: 0.001
freeze_backbone: true
